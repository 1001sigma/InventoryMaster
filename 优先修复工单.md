# 优先修复工单（Prioritized Ticket List）

生成日期: 2026-06-30
作者: GitHub Copilot（基于代码审计）

此文档列出基于代码审计的优先修复清单（按优先级排序），包含问题描述、受影响代码位置、推荐修复步骤及估算工时（人日）。请根据项目资源和发布节奏调整优先级。

---

## 1) 修复：增量/全量同步导致的数据丢失与事务安全
- 优先级：P0（最高）
- 严重性：关键（可能造成数据丢失）
- 问题概述：多处同步路径执行 delete + insert/逐条 insert/update 而未保证事务原子性；import/save 操作在大量单条 DAO 调用中间抛错会导致部分写入。
- 受影响文件（示例）：
  - app/src/main/java/com/example/inventorymaster/data/repository/InventoryRepositoryImpl.kt (saveImportDataWithResolution、importExcelData 等，约 lines 173-237)
  - app/src/main/java/com/example/inventorymaster/data/dao/StockRecordDao.kt (overwriteSessionData 已有 @Transaction)
  - app/src/main/java/com/example/inventorymaster/data/sync/FullDownloadStrategy.kt (使用 overwriteSessionData，但其他策略未必)
- 修复建议：
  1. 将 saveImportDataWithResolution 的整个导入/保存流程包装成单个 Room 事务（使用 RoomDatabase.withTransaction 或 在 DAO 中添加 @Transaction 方法）。
  2. 将逐条循环替换为批量 DAO 方法（insertRecords/updateRecords），减少 IO 与事务边界。
  3. 审查所有 deleteRecordsBySession + 后续 insert 路径，统一改为调用 overwriteSessionData 或在同一事务内执行。
- 估算工时：2.5 人日（实现 + 本地测试 + 回归）
- 建议标签：bug, high-severity, db, sync

---

## 2) 修复：增量锚点与时间戳逻辑（防止遗漏/倒退）
- 优先级：P0
- 严重性：关键（会导致同步不一致或遗漏记录）
- 问题概述：客户端使用 local lastUpdateTime/maxOf 作为锚点，服务端可能覆盖时间或时钟漂移；查询条件使用严格 `>` 会遗漏同毫秒时间戳的记录；缺乏防倒退策略。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/sync/IncrementalUploadStrategy.kt
  - app/src/main/java/com/example/inventorymaster/data/repository/InventoryRepositoryImpl.kt (pullNewData 路径)
  - app/src/main/java/com/example/inventorymaster/data/sync/TimestampManager.kt (存在于 data/sync，管理锚点)
  - 文档：同步优化.md
- 修复建议：
  1. 在服务端响应中使用服务端提供的 authoritative anchor（例如：response 中返回 server_anchor 或 max_server_time），客户端采用该时间作为锚点。
  2. 在客户端存储两个独立锚点：lastPullAnchor 和 lastPushAnchor（TimestampManager 需要扩展），并在保存时采用 max(current, new) 保证单调不回退。
  3. 查询改为使用 `>=` 并在接收端实施去重（基于 uuid）以避免重复处理。
  4. 在记录 DTO 中同时携带 clientUpdateTime 和 serverUpdateTime，明确冲突规则。
- 估算工时：3 人日（包含服务端接口协作/契约更新 + 客户端改造 + 测试）
- 建议标签：bug, sync, protocol, medium-risk

---

## 3) 修复：不安全的服务器响应解析与类型转换
- 优先级：P0 → P1（高）
- 严重性：高（可能导致崩溃或错误状态）
- 问题概述：代码对 Retrofit 响应做 Any/Map 强转（例如 IncrementalUploadStrategy 中 response.body()?.get("results") as? List<Map<String, Any>>），易抛出 ClassCastException，且缺乏严格校验和错误处理。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/sync/IncrementalUploadStrategy.kt (lines ~63-71)
  - 其他基于原始 Map 解析的 sync 代码
- 修复建议：
  1. 定义强类型 DTO（PushResponse、RecordResult 等）并在 Retrofit 接口中使用（Moshi/Gson 注解）。
  2. 删除 Any/Map 强转，使用 Kotlin 空安全与可空校验，遇到不符合契约的返回应失败并触发重试策略。
  3. 在 UI/日志中返回结构化错误，避免不确定的兼容性分支（见下条）。
- 估算工时：1.5 人日（接口模型定义 + 代码替换 + 单元测试）
- 建议标签：bug, network, api

---

## 4) 修复：回退兼容路径错误地标记所有脏数据为已同步
- 优先级：P0
- 严重性：关键（会丢失未被服务端实际处理的数据）
- 问题概述：IncrementalUploadStrategy 在无法解析新格式时会将所有 dirtyRecords 视为成功并调用 stockRecordDao.markAsSynced(successIds)（兼容分支），这是不安全的。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/sync/IncrementalUploadStrategy.kt (lines ~104-110)
- 修复建议：
  1. 移除“将所有脏数据全部标记为已同步”的兼容逻辑；若响应不符合预期，应返回错误并触发重试/人工干预。
  2. 使用 UUID 映射到本地 ID 的方式进行确认并仅标记服务端明确确认的记录。
  3. 在服务端实现��� uuid 去重与幂等保证（若尚未）。
- 估算工时：1 人日（代码更改 + 测试）
- 建议标签：bug, data-loss, urgent

---

## 5) 优化：全文检索与搜索性能（FTS 支持）
- 优先级：P1
- 严重性：中（影响大数据集下的 UX）
- 问题概述：searchRecordsByQuery 使用多个 `%like%` 带前置通配符，无法走索引、对大表性能差。当前 join product_base 也增加开销。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/dao/StockRecordDao.kt (searchRecordsByQuery)
  - utils: 使用结巴分词的相关代码（若用于搜索）
- 修复建议：
  1. 为产品名/厂商/规格等文本字段建立 Room FTS 虚表（FTS4/FTS5），并将搜索路由到 FTS（MATCH）查询。
  2. 对需要模糊搜索的字段做分词/归一化（中文结巴集成到建立 FTS 索引前的预处理或自定义 tokenizer）。
  3. 对 UI 做分页 / 限制返回条数以避免一次加载整个表。
- 估算工时：3 人日（FTS 设计与迁移脚本 + 搜索改造 + 性能验证）
- 建议标签：enhancement, performance, search

---

## 6) 改进：将长时同步任务迁移到 WorkManager
- 优先级：P1
- 严重性：中（生命周期/可靠性问题）
- 问题概述：目前从 UI/ViewModel 发起的同步可能会因为进程/Activity 生命周期中断，建议使用 WorkManager 保证在后台可靠执行并支持重试策略。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/viewmodel/SyncViewModel.kt（触发点）
  - app/src/main/java/com/example/inventorymaster/data/sync/SyncOrchestrator.kt（可复用的执行器）
- 修复建议：
  1. 创建 SyncWorker（Worker/CoroutineWorker），在 Worker 内调用 SyncOrchestrator.execute。
  2. SyncViewModel 只负责将任务 enqueue 至 WorkManager 并观察 Work 状态。
  3. 使用 WorkManager 的 backoff 策略或内置重试逻辑。
- 估算工时：2 人日（Worker 编写 + UI 适配 + 测试）
- 建议标签：enhancement, background, reliability

---

## 7) 改进：引入依赖注入（Hilt）以提升可测试性与维护性
- 优先级：P2
- 严重性：低-中（架构改善，长期收益）
- 问题概述：当前 InventoryApplication 手动创建单例（DB、Repository、SettingsRepository），ViewModel 构造分散，batchscanner 直接 new AndroidViewModel，影响测试与维护。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/InventoryApplication.kt
  - 各 ViewModel / Factory（例如 AppViewModelFactory）
- 修复建议：
  1. 使用 Hilt 注入 AppDatabase、Repository、SettingsRepository、TimestampManager 等单例。
  2. 迁移 ViewModel 注入为 @HiltViewModel，移除手工工厂与 direct AndroidViewModel new 操作。
  3. 为测试提供 TestModule 替换实现。
- 估算工时：4 人日（逐步迁移 + 修复注入问题 + 测试）
- 建议标签：refactor, architecture, testing

---

## 8) 改进：网络安全（HTTPS、身份验证和限制本地网关风险）
- 优先级：P1
- 严重性：高（数据传输在医疗场景需保证安全）
- 问题概述：当前通过 IP 直连本地 Python 网关，未见 TLS/认证保护；上传 PDF/数据存在被劫持风险。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/network/InventoryApiService.kt
  - app/src/main/java/com/example/inventorymaster/data/repository/InventoryRepositoryImpl.kt（uploadPythonTaskPdf）
- 修复建议：
  1. 优先支持 HTTPS（证书）或在网关上启用 TLS。若使用自签证书，提供证书安装/配对流程或使用 certificate pinning。
  2. 增加基础认证或签名（API key / HMAC）以防伪造请求。
  3. 对敏感接口（导出/上传）加权限/用户确认。
- 估算工时：2 人日（客户端改造 + 文档 + 与网关协作）
- 建议标签：security, network, compliance

---

## 9) 改进：记录级别的版本/冲突字段与更健壮的冲突策略
- 优先级：P2
- 严重性：中
- 问题概述：目前 StockRecord 仅有 lastUpdateTime 与 syncStatus，缺少变更计数或版本号用于确定冲突优先级；服务器端曾存在覆盖客户端时间的历史，需更明确的冲突策略。
- 受影响文件：
  - app/src/main/java/com/example/inventorymaster/data/entity/StockRecord.kt
  - 同步相关策略（IncrementalUpload/Download）
- 修复建议：
  1. 增加 version/sequence 字段或 lastChangeCounter，或同时使用 clientUpdateTime 和 serverUpdateTime。
  2. 明确定义冲突策略（server-wins、client-wins、merge-by-field）并在策略实现处统一执行。
  3. 考虑 tombstone（删除记录追踪）以支持删除同步。
- 估算工时：3 人日（模型修改 + DAO 迁移 + 同步逻辑更新 + 迁移测试）
- 建议标签：enhancement, data-model

---

## 10) 改进：日志与隐私处理（去标注生产日志）
- 优先级：P2
- 严重性：低-中（合规与运维）
- 问题概述：代码中使用 Log.d/w/e 输出可能泄露产品/设备/用户信息；生产构建应限制详细日志。
- 受影响文件（示例）：
  - app/src/main/java/com/example/inventorymaster/data/repository/InventoryRepositoryImpl.kt（日志 DEBUG_SAVE）
  - app/src/main/java/com/example/inventorymaster/data/sync/IncrementalUploadStrategy.kt（Log.w/Log.e）
- 修复建议：
  1. 使用集中化 Logger 封装，根据 BuildConfig.DEBUG 控制详细度。
  2. 在 release 建构中移除或模糊敏感字段（productKey、uuid、PII）。
  3. 集成 Crashlytics/Sentry 用于生产崩溃监控（仅发送必要上下文）。
- 估算工时：1 人日
- 建议标签：ops, privacy, logging

---

# 附：实施建议顺序（短期 30 天内）
1. 立即修复事务/导入原子性（Ticket #1）
2. 移除危险的兼容性标记逻辑（Ticket #4）
3. 强类型化网络响应与错误处理（Ticket #3）
4. 引入服务器-客户端锚点改进并保证单调性（Ticket #2）
5. 将同步迁移到 WorkManager（Ticket #6）
6. 性能/FTS 改造（Ticket #5）
7. 网络安全与认证（Ticket #8）
8. DI/Hilt 迁移（Ticket #7）
9. 记录版本/冲突策略（Ticket #9）
10. 日志与隐私清理（Ticket #10）

---

如果你希望，我可以接下来：
- 生成对应每个 Ticket 的 GitHub Issue（需要确认 repo owner/name 权限），或
- 提交一个初始 patch（PR）实现 Ticket #1（将 saveImportDataWithResolution 包装到事务并增加批量 DAO 操作）。

